<!-- Contact Modal Component -->
<div id="contact-modal" class="contact-modal-overlay hidden">
  <div class="contact-modal-container">
    <div class="contact-modal-header">
      <h2 class="contact-modal-title">Contactez Rayonis</h2>
      <button id="close-modal" class="contact-modal-close" aria-label="Fermer">
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <line x1="18" y1="6" x2="6" y2="18"></line>
          <line x1="6" y1="6" x2="18" y2="18"></line>
        </svg>
      </button>
    </div>
    <div class="contact-modal-body">
      <iframe 
        src="https://rayonis.app.n8n.cloud/form/7027c65c-ab47-4823-80b8-f9132c7866cb" 
        width="100%" 
        height="600" 
        frameborder="0"
        title="Formulaire de contact Rayonis"
      ></iframe>
    </div>
  </div>
</div>

<style>
  .contact-modal-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.7);
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 9999;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .contact-modal-overlay.show {
    opacity: 1;
  }

  .contact-modal-overlay.hidden {
    display: none;
  }

  .contact-modal-container {
    background: white;
    border: 2px solid #000;
    box-shadow: 8px 8px 0 rgba(0, 0, 0, 0.2);
    max-width: 600px;
    width: 90%;
    max-height: 90vh;
    overflow: hidden;
    transform: scale(0.9);
    transition: transform 0.3s ease;
  }

  .contact-modal-overlay.show .contact-modal-container {
    transform: scale(1);
  }

  .contact-modal-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem;
    border-bottom: 2px solid #000;
    background: #2dd4bf;
  }

  .contact-modal-title {
    font-family: 'Space Mono', monospace;
    font-weight: 700;
    font-size: 1.25rem;
    margin: 0;
    color: #000;
  }

  .contact-modal-close {
    background: white;
    border: 2px solid #000;
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .contact-modal-close:hover {
    background: #000;
    color: white;
    transform: translate(2px, 2px);
  }

  .contact-modal-body {
    padding: 0;
    overflow-y: auto;
    max-height: calc(90vh - 80px);
  }

  .contact-modal-body iframe {
    display: block;
    width: 100%;
    min-height: 600px;
    border: none;
  }

  @media (max-width: 640px) {
    .contact-modal-container {
      width: 95%;
      max-height: 95vh;
    }

    .contact-modal-header {
      padding: 1rem;
    }

    .contact-modal-title {
      font-size: 1rem;
    }

    .contact-modal-close {
      width: 36px;
      height: 36px;
    }

    .contact-modal-body iframe {
      min-height: 500px;
    }
  }
</style>

<script>
  // Open modal
  document.addEventListener('DOMContentLoaded', () => {
    const modal = document.getElementById('contact-modal');
    const closeButton = document.getElementById('close-modal');
    const triggers = document.querySelectorAll('[data-contact-modal]');

    // Open modal when clicking contact buttons
    triggers.forEach(trigger => {
      trigger.addEventListener('click', (e) => {
        e.preventDefault();
        modal?.classList.remove('hidden');
        setTimeout(() => {
          modal?.classList.add('show');
        }, 10);
        document.body.style.overflow = 'hidden';
      });
    });

    // Close modal on close button
    closeButton?.addEventListener('click', () => {
      closeModal();
    });

    // Close modal on overlay click
    modal?.addEventListener('click', (e) => {
      if (e.target === modal) {
        closeModal();
      }
    });

    // Close modal on escape key
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && modal?.classList.contains('show')) {
        closeModal();
      }
    });

    function closeModal() {
      modal?.classList.remove('show');
      setTimeout(() => {
        modal?.classList.add('hidden');
        document.body.style.overflow = '';
      }, 300);
    }
  });
</script>
